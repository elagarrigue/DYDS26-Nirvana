Plan
1.
Cambiar el contrato de detalle de película de id a title en toda la cadena de detalle:
◦
GetMovieDetailsUseCase.getMovieDetails(id: Int) -> getMovieDetails(title: String)
◦
MoviesRepository.getMovieDetail(id: Int) -> getMovieDetail(title: String)
◦
MoviesRemoteDataSource.getMovieDetails(id: Int) -> getMovieDetails(title: String)
◦
DetailViewModel.getMovieDetail(id: Int) -> getMovieDetail(title: String)
2.
Ajustar navegación para enviar título al detalle:
◦
En Navigation.kt, reemplazar movieId por movieTitle en la ruta.
◦
Hacer URL-encode/decode del título para evitar problemas con espacios/símbolos.
◦
HomeScreen no cambia funcionalmente (la lista sigue igual); solo cambia el argumento que se pasa al navegar.
3.
Separar clientes remotos por proveedor:
◦
Mantener TMDB para getPopularMovies.
◦
Crear cliente HTTP OMDB (host = www.omdbapi.com, query apikey=...).
◦
Evitar reutilizar el cliente TMDB porque hoy inyecta api_key y host fijos.
◦
Crear `TMDBMoviesExternalSource` y `OMDBMoviesExternalSource` como implementaciones concretas por proveedor.
4.
Agregar broker de fuentes externas:
◦
Crear `MovieExternalSourceBroker` que implemente `MovieExternalSource`.
◦
El broker debe depender de `TMDBMoviesExternalSource` y `OMDBMoviesExternalSource`.
◦
Implementar en el broker la lógica del requerimiento (orquestación/merge/fallback entre proveedores).
5.
Incorporar datasource/modelo OMDB:
◦
Crear OmdbRemoteMovie (campos serializables de OMDB).
◦
Implementar request por título (?t=<title>).
◦
Manejar respuesta “not found” (OMDB responde Response: "False" + Error) sin romper flujo.
6.
Resolver mapeo y enriquecimiento de detalle:
◦
Estrategia recomendada: seguir devolviendo Movie y enriquecer campos existentes con OMDB cuando aplique.
◦
Si se quieren mostrar campos nuevos (ej. runtime, genre, imdbRating), extender Movie + UI + strings.
◦
Definir regla de merge TMDB vs OMDB (qué fuente tiene prioridad por campo).
7.
Actualizar fallback de caché local:
◦
Como el detalle ya no entra por id, en local cambiar búsqueda a title (idealmente normalizada, case-insensitive).
◦
Mantener el comportamiento actual: si falla remoto, intentar cache.
8.
Refactor de DI/config:
◦
Inyectar ambos data sources (TMDB + OMDB) en MoviesRepositoryImpl.
◦
Mover keys de API fuera del código hardcodeado (gradle properties/env vars) para TMDB y OMDB.
9.
Actualizar tests unitarios:
◦
Fakes (FakeMoviesRemoteDataSource, FakeMoviesRepository) para trabajar con title.
◦
Tests de use case/viewmodel/repository para:
▪
llamada por título,
▪
éxito remoto OMDB,
▪
fallback a caché por título,
▪
título no encontrado en OMDB.
◦
Agregar tests unitarios específicos del broker (`MovieExternalSourceBroker`) cubriendo:
▪
ruteo/orquestación correcta entre `TMDBMoviesExternalSource` y `OMDBMoviesExternalSource`,
▪
reglas de merge/prioridad por campo,
▪
casos de fallback y errores controlados.
10.
Verificación final:
◦
Ejecutar suite de tests.
◦
Prueba manual: abrir home, entrar a detalle de una película conocida, validar que los datos de detalle vengan por título y que la lista no cambió.
